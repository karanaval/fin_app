<p></p>
<div class="container">
    <h1><%= @category_name %></h1>
    <h4><%= @dates_period %></h3>
</div>

<div class="container">
    <div class="row">
        <div class="my-custom-scrollbar my-custom-scrollbar-primary" style="width:317px">
            <table class="table table-striped tableFixHead" style="width:300px">
                <thead>
                    <tr>
                        <th scope="col">#</th>
                        <th scope="col">Category name</th>
                        <th scope="col">Amount</th>
                    </tr>
                </thead>
                <tbody>
                    <% for i in (0...@categories_names.length) do %>
                        <tr>
                            <th scope="row"><%= i + 1 %></th>
                            <td><%= @categories_names[i] %></td>
                            <td><%= @amounts_per_categories[i] %></td>
                        </tr>
                    <% end %>
                </tbody>
            </table>
        </div>
        <div class="col" style="height:400px">
            <%= render "chart_by_category" %>
        </div>
    </div>
</div>
